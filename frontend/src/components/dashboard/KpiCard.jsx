import React from 'react';

// Ce composant est un exemple minimal de carte KPI.
// Pour afficher des données réelles, connectez ce composant à l'API backend (ex: via fetch ou axios).

function KpiCard() {
  return <div>KPI Card</div>;
}

export default KpiCard;